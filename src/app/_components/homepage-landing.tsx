import Link from "next/link";
import type { ReactNode } from "react";
import { SiteBrand } from "@/components/site-brand";
import { formatSwissDate, formatSwissDateTime } from "@/lib/date-format";
import type { JobSearchResult } from "@/lib/job-catalog";
import { getLandingPath, TOP_LANDING_PAGES } from "@/lib/landing-pages";
import { DirectHireOpportunityFeed } from "@/components/direct-hire-opportunity-feed";

const EMPLOYER_MENU_ITEMS = [{label:"Arbeitgeber-Login",href:"/arbeitgeber/login"},{label:"Preise & Pakete",href:"/arbeitgeber/preise"},{label:"Kandidatenzugang",href:"/arbeitgeber/kandidaten"},{label:"Support kontaktieren",href:"/kontakt"}];
export interface HomepageLandingConfig { hero: ReactNode; sectionKicker: string; sectionTitle: string; keywordPlaceholder: string; }
export function HomepageLanding({initialData,config}:{initialData:JobSearchResult;config:HomepageLandingConfig}) { return (
  <div className="min-h-screen flex flex-col overflow-x-clip">
    <header className="trade-header border-b sticky top-0 z-30"><div className="container mx-auto px-4 sm:px-6 h-16 sm:h-[4.5rem] flex items-center justify-between gap-2"><Link href="/" className="flex items-center shrink-0"><SiteBrand /></Link><nav className="flex items-center gap-1 sm:gap-2 shrink-0"><details className="group relative hidden sm:block"><summary className="flex h-11 cursor-pointer list-none items-center border border-primary/35 bg-accent px-4 text-sm font-medium">Für Arbeitgeber <span aria-hidden="true" className="ml-2">⌄</span></summary><div role="menu" aria-label="Für Arbeitgeber" className="absolute right-0 top-[calc(100%+0.4rem)] z-50 hidden min-w-56 border border-border bg-background p-1 shadow-xl group-open:block">{EMPLOYER_MENU_ITEMS.map(item=><Link key={item.href} href={item.href} role="menuitem" className="block px-3 py-2 text-sm hover:bg-accent">{item.label}</Link>)}</div></details><Link href="/arbeitgeber/preise" className="inline-flex h-11 items-center justify-center border border-primary/35 px-2.5 text-xs font-medium sm:px-4 sm:text-sm"><span className="sm:hidden">Betriebe</span><span className="hidden sm:inline">Angebot für Betriebe</span></Link></nav></div></header>
    <main id="main-content" className="flex-1">
      <section className="trade-hero relative z-20 overflow-visible pt-10 pb-6 sm:pt-14 sm:pb-9 md:pt-20"><div className="container mx-auto max-w-6xl px-4 sm:px-6">{config.hero}<form className="relative z-30" action="/" method="get"><div className="search-bench min-w-0"><div className="search-field"><label htmlFor="landing-keyword" className="search-field__label">Beruf oder Stichwort</label><div className="search-field__control"><svg viewBox="0 0 24 24" aria-hidden="true" focusable="false" className="h-5 w-5 text-primary"><circle cx="11" cy="11" r="7" fill="none" stroke="currentColor" strokeWidth="2"/><path d="m16.2 16.2 4 4" fill="none" stroke="currentColor" strokeWidth="2"/></svg><input id="landing-keyword" name="q" type="search" maxLength={80} placeholder={config.keywordPlaceholder} className="min-w-0 flex-1 bg-transparent text-base font-semibold outline-none"/></div></div><div className="search-field"><label htmlFor="landing-location" className="search-field__label">Arbeitsort</label><div className="search-field__control"><svg viewBox="0 0 24 24" aria-hidden="true" focusable="false" className="h-5 w-5 text-primary"><path d="M12 22s7-6.1 7-13a7 7 0 1 0-14 0c0 6.9 7 13 7 13Z" fill="none" stroke="currentColor" strokeWidth="2"/><circle cx="12" cy="9" r="2.5" fill="none" stroke="currentColor" strokeWidth="2"/></svg><input id="landing-location" name="loc" type="search" maxLength={80} placeholder="Wo? (Ort, Kanton oder PLZ)" className="min-w-0 flex-1 bg-transparent text-base font-semibold outline-none"/></div></div><input type="hidden" name="radiusKm" value="25"/><button type="submit" className="h-12 w-full bg-primary px-6 text-base font-bold text-primary-foreground md:w-auto sm:px-8">Jobs suchen</button></div></form></div></section>
      <section className="results-workbench relative z-10 pt-8 pb-16 sm:pt-12"><div className="container mx-auto max-w-6xl px-4 sm:px-6"><div className="mb-5 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p className="eyebrow mb-2">{config.sectionKicker}</p><h2 className="text-2xl font-black text-foreground sm:text-3xl">{config.sectionTitle}</h2>{initialData.scrapedAt&&<p className="mt-1 text-xs text-slate-500">Datenstand: {formatSwissDateTime(initialData.scrapedAt)}</p>}</div>{initialData.total>0&&<span className="font-mono text-xs font-semibold uppercase tracking-wider text-muted-foreground">{initialData.jobs.length} von {initialData.total} Stellen</span>}</div>{initialData.opportunities.length>0&&<div className="source-panel mb-4 px-4 py-3 text-sm text-foreground" role="status"><p className="font-semibold">{initialData.total} konkrete {initialData.total===1?"Stelle":"Stellen"} und {initialData.opportunities.length} Direktanstellungsprofile</p><p className="mt-1">Die Profile sind keine offenen Stellen. Arbeitgeber werden passend gesucht; die Anstellung erfolgt direkt beim jeweiligen Arbeitgeber.</p></div>}{!initialData.scrapedAt&&<div className="source-panel mb-4 px-4 py-3 text-sm text-foreground"><p className="font-semibold">Datenstand: unbekannt</p><p className="mt-1">Bis zum nächsten verifizierten Abruf zeigt die Startseite keine ungeprüften Inserate.</p></div>}{initialData.jobs.length===0&&initialData.opportunities.length===0?<div className="border border-border bg-card p-6 text-center shadow-sm"><p className="font-semibold text-foreground">Zurzeit sind keine verifizierten Stellen sichtbar</p><p className="mt-1 text-sm text-muted-foreground">Starte eine Suche oder prüfe die Seite nach dem nächsten Abruf erneut.</p></div>:initialData.jobs.length>0?<div className="space-y-3 sm:space-y-4">{initialData.jobs.map(job=><Link key={job.id} href={`/jobs/${job.id}`} className="block group"><article className="job-card p-5 sm:p-6"><h3 className="text-base font-bold text-foreground group-hover:text-primary sm:text-xl">{job.title}</h3><div className="job-facts mt-3"><div className="job-fact"><span className="job-fact__value">{job.location}</span><span className="job-fact__label">Ort</span></div><div className="job-fact"><span className="job-fact__value">{job.workload}</span><span className="job-fact__label">Pensum</span></div><div className="job-fact"><span className="job-fact__value">{job.type}</span><span className="job-fact__label">Anstellungsart</span></div><div className="job-fact"><span className="job-fact__value">{formatSwissDate(job.datePosted)}</span><span className="job-fact__label">Publiziert</span></div></div><p className="mt-3 line-clamp-2 text-sm text-muted-foreground">{job.description}</p></article></Link>)}</div>:null}<DirectHireOpportunityFeed opportunities={initialData.opportunities}/><div className="mt-10 sm:mt-12"><h3 className="mb-3 text-lg font-bold text-foreground">Beliebte Suchseiten</h3><div className="grid grid-cols-1 gap-2 sm:grid-cols-2 lg:grid-cols-3">{TOP_LANDING_PAGES.slice(0,12).map(item=><Link key={`${item.role}-${item.canton}`} href={getLandingPath(item)} className="link-tile flex items-center px-3 py-2 pr-9 text-sm text-foreground hover:border-primary/50 hover:text-primary">{item.title}</Link>)}</div></div></div></section>
    </main>
  </div>
); }
